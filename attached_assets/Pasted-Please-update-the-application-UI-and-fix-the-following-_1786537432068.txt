Please update the application UI and fix the following issues across the entire project:

### 1. Add spacing between sidebar menu items

* Increase the vertical spacing/padding between each menu item in the sidebar.
* Keep the spacing consistent for all sidebar items and nested items.
* Make sure the sidebar remains clean and visually balanced on desktop and mobile.
* Do not change the existing colors, icons, or overall design unless necessary.

### 2. Fix the toggle

* Fix the toggle/switch component so it works correctly when clicked.
* Make sure the visual state always matches the actual application state.
* Ensure both ON and OFF states are clearly visible.
* Make sure it works correctly with mouse/touch interaction.
* Check that there are no state, event-handler, or styling issues causing the toggle to get stuck or behave inconsistently.
* Preserve the existing design/style of the toggle.

### 3. Fix Arabic title direction in ALL tables on ALL pages

This is especially important.

* When the application is in Arabic/RTL mode, **all table column titles/headers must display correctly from right to left**.
* Apply the fix globally to **every table on every page**, not just one specific table.
* Arabic table headers should have the correct RTL text direction and alignment.
* Make sure mixed Arabic/English titles are also displayed correctly.
* The table structure/order should remain logically correct while the Arabic text direction is fixed.
* Do not break the English/LTR version.
* When switching between Arabic and English, the table headers should automatically use the correct direction and alignment.
* Check all reusable table components, shared components, and page-specific tables so the fix is applied consistently everywhere.

### Important requirements

* First inspect the existing codebase and identify the shared components/styles responsible for the sidebar, toggle, and tables.
* Prefer fixing the issue at the shared/global component level instead of duplicating fixes on individual pages.
* Do not introduce unnecessary changes to the existing UI.
* Make sure the changes are responsive.
* Test the affected pages in both **English (LTR)** and **Arabic (RTL)**.
* Verify that every table header is correct in Arabic after the changes.
* Verify that the sidebar spacing and toggle still work after changing language/direction.
* Do not remove or break any existing functionality.

After making the changes, review the entire application for similar RTL table-header issues and fix them consistently.
